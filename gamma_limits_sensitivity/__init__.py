'''
This is the hard working code in order to calculate ULs, sensitivities,
and time to detections.
'''
import matplotlib.pyplot as plt


def upper_limit(on, off, alpha, l_lim, A_eff):
    figure = plt.figure()
    return [figure]
